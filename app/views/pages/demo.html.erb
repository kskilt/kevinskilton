<% content_for :title, "Code Example: Model Selection — Kevin Skilton" %>

<section class="hero-panel">
  <div class="hero-main">
    <p class="section-kicker">Code example</p>
    <h1 class="hero-title">Letting people choose their own AI model — safely.</h1>
    <p class="hero-copy">A real feature I shipped, told three ways: the story behind it, a working demo of the trickiest part, and the shape of the code.</p>
    <p class="mt-3 text-sm italic text-slate-500">The code shown is a from-scratch reconstruction of the real pattern — the literal source belongs to a former/current employer and isn't mine to publish.</p>
  </div>
  <%= render "shared/built_with", technologies: [ "Rails 8", "Turbo Streams", "Tailwind" ] %>
</section>

<section class="content-panel">
  <%= render "pages/demo_panel" %>
</section>
